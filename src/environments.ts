/**
 * I have decided to put the URIs here instead of in an .env file to speed up the test review process.
 */

const environments = {
  BASE_URL: 'http://localhost:3200/api',
};

export default environments;
